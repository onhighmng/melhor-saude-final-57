#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Pre-commit hooks disabled temporarily
# Uncomment to re-enable:
# npx lint-staged
# npm run test:staged

echo "Pre-commit hooks are disabled. Run 'npm run lint' and 'npm test' manually."
